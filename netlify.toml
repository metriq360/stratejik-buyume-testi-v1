[build]command = "npm run build"publish = "dist"functions = "netlify/functions"[functions]node_bundler = "esbuild"external_node_modules = ["node-fetch", "nodemailer"][[redirects]]from = "/.netlify/functions/*"to = "/.netlify/functions/:splat"status = 200
